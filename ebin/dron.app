{application, dron, [{description, "Job Scheduler"},
                    {vsn, "1"},
                    {modules, [dron,
                               dron_api,
                               dron_config,
                               dron_consumer,
                               dron_coordinator,
                               dron_db,
                               dron_event_consumer,
                               dron_frag,
                               dron_hash,
                               dron_language,
                               dron_mnesia,
                               dron_monitor,
                               dron_pubsub,
                               dron_pool,
                               dron_scheduler,
                               dron_sup,
                               dron_worker,
                               dron_test]},
                    {env, []},
                    {mod, {dron, []}},
                    {applications, [kernel, stdlib, sasl]}]}.
